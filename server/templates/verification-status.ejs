<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <style>
    .container { 
      font-family: Arial, sans-serif; 
      max-width: 600px; 
      margin: 2rem auto; 
      padding: 2rem;
      border: 1px solid #eaeaea;
      border-radius: 8px;
      text-align: center;
    }
    .success { color: #4CAF50; }
    .error { color: #f44336; }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      border-radius: 4px;
      margin: 20px 0;
    }
    .meta {
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="container">
    <% if (success) { %>
      <h1 class="success">✓ Email Verified</h1>
      <p><%= message %></p>
      <p>You can now <a href="/login">log in</a>.</p>
      <meta http-equiv="refresh" content="3;url=/login">
    <% } else { %>
      <h1 class="error">Verification Failed</h1>
      <p><%= message %></p>
      <% if (showRetry) { %>
        <a href="/register" class="button">Register Again</a>
      <% } %>
    <% } %>
    <div class="meta">
      <p><%= appName %> · <%= new Date().toLocaleString() %></p>
    </div>
  </div>
</body>
</html>
